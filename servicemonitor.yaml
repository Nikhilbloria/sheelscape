apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shellscape-servicemonitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: shellscape
  endpoints:
    - port: http
      path: /metrics
      interval: 5s